# Gate estadístico · Ola 1 · pieza ① «carga sin FC» (session-RPE) · 2026-09-02

Modo GATE de requerimiento (no hay código). Re-derivé contra el código real: `StrainScorer.swift` (`trimpToStrain` :172-176, D=7201, redondeo a 2 dp; `fitStrainDenominator` :181-195; `hasEnoughData` :204-209), `ReadinessEngine.swift` (`ewmaReplay` :443-483, λa=0.25, λc=2/29; `gatePasses` :487-489; monotonía :294-315; `strainToLoad` :637-641), `SourceFusion.swift` (`mergeDaily` :31-45), `AppleLoadEstimator.swift` (`classify` :86-98), `AppModel+Strength.swift:164,194-198` (`endTs = startTs + elapsedSeconds()`; strain solo si `hrSamples.count >= 2` y `StrainScorer.strain` no-nil). Números: ln D = 8.88198; banda strain 10–12 ⇔ TRIMP 67.7–159.0.

## Veredicto numérico: CON HALLAZGOS — PASS condicionado a H1, H2, H3 y H7
La fórmula base (AU → k·AU → `trimpToStrain`) es correcta y se apoya en la inversa exacta que ya usa el ACWR. Tres decisiones del spec están mal derivadas (umbral de FC, «k absorbe la escala», estimador OLS) y una regla (overlay retroactivo) produce un veredicto falso durante ~4 semanas tras un import. Todo lo que propone cambiar un coeficiente es **RE-VERIFICAR en el hilo principal antes de actuar**.

## Hallazgos (rankeados por impacto)

| # | Cambio | Eje | Sev. | Re-derivación (esperado vs spec/código) | Cambio mínimo al requerimiento |
|---|---|---|---|---|---|
| H1 | Umbral «FC suficiente» (pregunta 5) | 7 calibración | **BUG** (en el spec) | Hoy la única puerta es `hasEnoughData` (≥600 muestras **o** ≥20 muestras que abarquen ≥600 s) + `count >= 2` en AppModel+Strength:194. Es un piso **absoluto de 10 min**, no cobertura. Edwards es lineal en tiempo: sesión de 50 min con reloj 12 min → TRIMP ≈ 24 en vez de 100 → strain 7.5 vs 10.9, se guarda como `hr` «medido», **no se pregunta RPE** (R1) y el par entra a la calibración con TRIMP subestimado (arrastra k hacia abajo). Cobertura c: TRIMP·c → strain: c=0.5 → 9.3 (−1.6, una banda); c=0.8 → 10.4 (−0.5). | `coverage = Σ gaps plausibles (<300 s, mismo criterio que `HRZones.medianInterval`) / elapsedSeconds`. **Medido ⇔ `hasEnoughData` ∧ coverage ≥ 0.8**; si no → `strainSource = .rpe` (se pregunta). Los pares de calibración exigen el mismo umbral. Test: 50 min con 12 min de FC → `.rpe`; con 41 min → `.hr`. |
| H2 | Escala 6–10 directa «k lo absorbe» (R2, pregunta 2) | 1 fórmula | **BUG** (en el spec) | k es multiplicativo; el desfase de escala es **aditivo** y no se absorbe. CR-10 de Foster va 0–10 (3 moderado, 5 duro, 7 muy duro, 10 máximo); la fila 6–10 anclada en RIR (6 = 4 RIR) tiene rango dinámico 10/6 = **1.67×** vs ≥ **2.5×** en Foster. Con k=0.25: 60 min a RPE 6 → 10.24; 60 min a RPE 10 → 11.86: una hora suave y una hora al máximo difieren **1.6 pts** de 21. Edwards para 30 min zona 1 = 7.99, pero 30 min × RPE 6 = **9.05**. Sesgo: infla lo suave, comprime lo duro; el ACWR pierde justo la variación que Foster 1998 (monotonía) necesita. | Mapeo afín explícito antes de AU: `cr10 = 1.5·rpe − 5` (6→4 «algo duro», 8→7, 9→8.5, 9.5→9.25, 10→10; rango 2.5×). Anclas las fija /cso; la numérica exige que exista **un** mapeo afín, no la identidad. `(rpe−5)×2` también sirve (rango 5×; 6→2 «fácil» me parece bajo para 4 RIR). |
| H3 | `fitTrimpPerAU` por OLS por el origen (pregunta 3) | 3 robustez | **DÉBIL** | OLS-origen `k = Σ(au·trimp)/Σ(au²)` pesa cada par por au²: la sesión más larga manda. 4 pares normales (k≈0.258) + 1 sesión de 3 h a RPE 10 con TRIMP 300 → **k = 0.111** (−57 %); mediana de razones → 0.250. Un par corto/intenso apenas mueve ninguno (0.267 vs 0.262). El clamp [0.05, 1.0] no protege: 0.111 está dentro. Pseudo-replicación: k es una constante **personal**, así que «mismo usuario» es el diseño, no un defecto; pero las sesiones son correlacionadas en el tiempo (misma rutina, misma semana) → n_eff < 5. | Estimador = **mediana de razones trimp/au** (= Theil–Sen por el origen; implementable a mano). `minCalibrationPairs = 5` se acepta, con clamp. Los pares pasan H1. Test: `testFitIgnoresOneExtremePair` (5 pares, uno 10× → k dentro de ±5 % del resto). |
| H4 | Estabilidad del ACWR al cambiar k (pregunta 3) | 7 calibración | **DÉBIL** | Sesiones viejas con k viejo y nuevas con k nuevo en la misma serie: `strainToLoad` es la inversa exacta, así que un salto de k es un salto **lineal** de carga. Simulé rutina fija 3×/sem (strain 10.91, descansos 0), ACWR estable 0.75: k 0.25→0.5 en la semana 7 → **0.92** a las 2 semanas (→ 1.5 en el límite, «Ramping fast» con la misma rutina); k 0.25→0.125 → **0.55** («Easing off»). | Refit solo cuando el nº de pares **se duplica** (5, 10, 20, 40) y aceptar el nuevo k solo si |Δ| > 15 %; registrar `trimpPerAU` usado en la sesión (columna o en el snapshot) para que un refit futuro pueda recomputar `.rpe` de una vez si el dueño lo decide. Tendencias enseña «k recalibrado el D» (copy /ux). |
| H5 | Regla por día `max(Apple, Σ Cénit)` (pregunta 4) | 1 fórmula | **DÉBIL** | Correcto cuando Apple **contiene** la misma sesión (Watch Workout): evita doble conteo. Incorrecto cuando son entrenos **distintos**: carrera medida 12.0 (TRIMP 159) + pesas estimadas 10.9 (TRIMP 100) → día = 12.0; la suma honesta en TRIMP es 259 → **13.1**; la sesión de pesas desaparece del ACWR. Σ de sesiones Cénit en TRIMP entre sí sí es correcta (aditividad, disjuntas por construcción). | Suma en TRIMP cuando el intervalo `[startTs, endTs]` de la sesión Cénit **no traslapa** ningún HKWorkout usado en el strain de Apple; `max` cuando traslapa (mismo patrón de `mergeSleepSessions`). Si el snapshot no puede cargar los intervalos en esta ola, documentar el sub-conteo como decisión y dejar el test `testDisjointWorkoutsAddInTrimp` como ⬜ pendiente. |
| H6 | `rest(0)` → `nil` con sesión sin strain; nil vs 0 en EWMA y monotonía (pregunta 4) | 6 independencia | SÓLIDO | Verificado en `ewmaReplay` :464-478: `nil` ⇒ sin fold, sin `coverageDays`, `activeInWindow=false`; `0` ⇒ fold que decae el agudo. Convertir un falso 0 en hold es la dirección correcta (H1 de la consolidación). Monotonía :300-309 salta nil y exige ≥4 valores en la semana → sin efecto raro. Sesgo por estimadas: semana entera +40 % en TRIMP → ACWR 0.75→**0.87**; −40 % → **0.58**: una banda, no un pico falso. Cambio de tasa de captura de RPE 50 %→100 % → 0.75→0.86. Un usuario que **nunca** califica: todas sus sesiones son hold, los descansos 0 → `activeInWindow` cae a 0 → ACWR nil (honesto). | Ninguno numérico. Mantener A9 de ux-A (conteo de estimadas y de «sin calificar» en el pie de Carga) como criterio obligatorio, es la única defensa contra el sesgo sistemático (Lolli 2019 / Impellizzeri 2020: el ratio acoplado ya infla la correlación; una fuente sesgada lo empeora). |
| H7 | Overlay retroactivo de 5 años (pregunta 6) | 8 edge | **BUG** (en el spec) | El overlay sintetiza filas **solo en días con sesión** (paso 5): en la era importada no hay descansos 0 (no hay fila Apple), todo hueco es hold → el EWMA no decae entre sesiones y siembra agudo=crónico=**carga por sesión** (100), no dosis diaria (≈43 en estado estable). Al empalmar con la era Apple (descansos 0): día 7 tras el import ACWR **0.52**, día 14 **0.51**, día 28 **0.64** vs 0.76 real → «Easing off» falso ~4 semanas (un día pesa <1 % en el crónico hasta 65 días). Rellenar ceros fabricaría descanso; tampoco. | El overlay sintetiza filas **solo a partir de la primera fila base** (primer día Apple/computed con `strain != nil`) o, sin base, dentro de `[hoy − 56, hoy)`. El historial anterior alimenta récords/1RM/volumen, no el ACWR. Test: 5 años importados + 28 días Apple → `acwr` dentro de ±0.05 del caso sin import. |
| H8 | `testRoundTripThroughStrainToTrimp` con ±1e-6 (arq-A) | 8 edge | **BUG** (en el test propuesto) | `trimpToStrain` redondea a 2 dp (:175). inv(10.91) = e^(10.91·8.88198/21) − 1 = 99.93, no 100. Error relativo máx = 0.005·lnD/21 = **0.21 %**; el test como está escrito falla. | Tolerancia relativa 0.3 % (o probar la inversa contra la fórmula sin redondeo). Suma de dos sesiones en TRIMP tras redondeo: error < 0.3 %, aceptable. |
| H9 | Default k = 0.25 (pregunta 1) | 7 calibración | DÉBIL (knob) | Con k=0.25 y escala directa: 50×8 → TRIMP 100 → **10.91** ✓ (banda 10–12); 30×6 → **9.05**; 90×9 → **12.57**; 45×5 → 9.57; 60×10 → 11.86. Contra Edwards real de fuerza (FC bajo 50 % HRR entre series): 60 min con 40 % zona 1 + 20 % zona 2 → TRIMP 48 → strain **9.2** ⇒ k implícito a RPE 8 = **0.10**; Banister 60 min a 55 % HRR → 9.75. k=0.25 supone que una sesión de pesas puntúa como 50 min de zona 2. Es un knob rotulado (correcto), pero su prior está al tope del rango plausible y la calibración lo va a bajar. | Con mapeo H2 (`1.5·rpe−5`): 50×8 → cr10 7 → 350 AU → k = 100/350 ≈ **0.29** (30×6 → 8.5; 90×9 → 12.85). Sin mapeo (si /cso lo rechaza): **k = 0.20** (50×8 → 10.4; 30×6 → 8.6; 90×9 → 12.1). Ambos rotulados «calibration default, not validated». RE-VERIFICAR. |
| H10 | Edges (pregunta 7) | 8 edge | SÓLIDO con 2 ajustes | Duración < 300 s → nil ✓. > 3 h: AU lineal, 180×10×0.25 → TRIMP 450 → 14.45 (sesión olvidada abierta). RPE fuera de [5,10] → nil, nunca clamp hacia arriba ✓. Dos sesiones el mismo día (una FC, una RPE): Σ TRIMP ✓ (H5). Cruce de medianoche: `DayKey.local(startTs)` asigna toda la sesión al día de inicio; Apple parte la FC por muestra → la comparación `max` es entre parciales. `elapsedSeconds` excluye pausas ✓ (FER-823). | Tope `maxDurationS = 3·3600` para estimar (copy ux-A edge 8). Sesión que cruza medianoche: día de inicio, documentado; test `testSessionCrossingMidnightKeysToStartDay`. |

## Tests re-ejecutados por mí
- **Ninguno — BLOCKED por instrucción del director** («NO corras swift test ni build»). La evidencia es la re-derivación a mano + simulación del `ewmaReplay` con las mismas λ (scripts en `scratchpad/rederive.py`, `ewma.py`, `seam.py`). Tests existentes que fijan lo que asumo y que `/implement` debe ver verdes sin editar: `testMissingDaysHoldEwma`, `testEwmaDecaysInRest`, `testActiveDaysPisoAtMinChronic`, `testAcwrSeriesLastPointMatchesEvaluate` (ReadinessEngineTests.swift:385-432).
- Tests nuevos que exijo además de los de arq-A: `testCoverageBelowThresholdFallsToRPE` (H1), `testAffineMapChangesEasyHardRatio` (H2), `testFitIgnoresOneExtremePair` (H3), `testImportedHistoryDoesNotSeedChronic` (H7), `testRoundTripToleranceIsRelative` (H8), `testDurationCapAtThreeHours` (H10).

## Disciplina de calibración (knobs)
- ✅ `defaultTrimpPerAU`, `rpeMin/rpeMax`, `minDurationS`, `minCalibrationPairs`, clamp `[0.05, 1.0]` rotulados «calibration default, /estadistico owns» → correcto, no es hallazgo (solo el valor, H9).
- ✅ `minActiveDays = 4`, `stepsRestMax/kcalRestMax` ya rotulados en el motor.
- ⚠️ «k absorbe el mapeo RPE→CR-10» (R2) es un knob disfrazado de propiedad matemática → H2.
- ⚠️ Umbral de cobertura de FC: hoy no existe como knob; nace con H1 rotulado (`minHRCoverage = 0.8`).

## Respuestas directas a las 7 preguntas
1. Sí, 50×8 cae en 10.91 con k=0.25; 30×6 → 9.05; 90×9 → 12.57. Default propuesto: 0.29 con mapeo afín, 0.20 sin él (H9).
2. Hay sesgo y k no lo absorbe (aditivo vs multiplicativo). Hace falta mapeo afín (H2).
3. OLS-origen no es robusto (H3); mediana de razones. Pseudo-replicación no aplica al fin (k personal) pero n_eff<5. Refit por duplicación + histéresis (H4).
4. `max` correcto para traslape, incorrecto para entrenos disjuntos (H5). nil-hold correcto en EWMA y monotonía; sesgo de estimadas ≈ una banda, no picos (H6).
5. Cobertura ≥ 0.8 de `elapsedSeconds` con gaps plausibles, además de `hasEnoughData` (H1).
6. Todo el historial entra a récords; al ACWR solo desde la primera fila base o ≤ 56 días (H7). El crónico tiene memoria efectiva de ~65 días, no 28.
7. Cubiertos en H10; el único ajuste numérico es el tope de 3 h.

## Para el CSO (validez científica / citas)
- Anclas del mapeo set-RPE (RIR, Zourdos 2016) → CR-10 de sesión (Foster 2001): ¿6 (4 RIR) ≈ 4 «algo duro» o ≈ 2 «fácil»? Decide el mapeo; yo solo exijo que sea afín, no identidad.
- Session-RPE de Foster se mide 30 min **después** de la sesión; aquí se captura en el recibo inmediato y se prellena con la media de RPE por serie. Validar que el prellenado no sesga hacia la última serie.
- Umbral de cobertura 0.8 es numérico (una banda de error); si hay literatura sobre pérdida de datos en TRIMP, mejor.
