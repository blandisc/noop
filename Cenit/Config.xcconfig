// Base build configuration for the Cenit target (referenced by project.yml).
// Currently empty: the exercise-media downloader no longer needs an API key — since FER-786 it
// fetches each exercise's baked `gifUrl` directly from ExerciseDB's public CDN, by native id.
